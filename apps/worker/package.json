{
  "name": "@billing/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Publishes the outbox, and later runs the jobs it schedules",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@billing/db": "*",
    "@billing/domain": "*",
    "@billing/platform": "*",
    "kysely": "^0.29.5"
  }
}
